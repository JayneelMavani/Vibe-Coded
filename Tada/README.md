# Tada

Minimal React (Babel standalone) to-do demo. Mounts the app in `index.html` and loads `app.jsx`.

How to view

- Open `index.html` in a browser. No build step required — uses CDN React and Babel standalone.
